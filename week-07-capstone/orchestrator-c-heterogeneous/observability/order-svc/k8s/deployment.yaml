# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-svc
  namespace: orders
  labels:
    app: order-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-svc
  template:
    metadata:
      labels:
        app: order-svc
    spec:
      containers:
        - name: order-svc
          image: order-svc:latest
          imagePullPolicy: Never  # image already built locally on the Docker Desktop daemon — never try to pull from a registry
          ports:
            - containerPort: 8080
          # no resource requests/limits set for now — Phase 1 goal is just "runs on K8s";
          # revisit if CPU throttling from limits interferes with the psutil-based measurement (see decisions.md D17)
---
apiVersion: v1
kind: Service
metadata:
  name: order-svc
  namespace: orders
  annotations:
    prometheus.io/scrape: "true"       # tells Prometheus's job to pick this Service up
    prometheus.io/path: "/metrics/prometheus"  # our new endpoint, not the old JSON one
    prometheus.io/port: "8080"
spec:
  selector:
    app: order-svc
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
