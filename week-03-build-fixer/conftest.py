# Empty on purpose. Its presence at the project root puts that root on
# sys.path, so tests can do `from src.calculator import ...` and
# `from scripts.logparse import ...` (src/ and scripts/ as namespace packages).
